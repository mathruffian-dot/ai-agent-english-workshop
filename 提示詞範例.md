# 🗣️ 研習應用提示詞範例（複製即用）

> 對象：AI Agent（Anti-Gravity / OpenCode）。把對應提示詞貼給 Agent，依需要替換 `〈〉` 裡的內容。
> 原則：第一次用先把身分寫進 `AGENTS.md`，之後每個任務都更精準。

---

## 〇、環境 / 設定

**0-1 建立英文老師專屬 AGENTS.md**
```
幫我在這個資料夾建立一份 AGENTS.md，內容：我是國中英文老師，所有回應用繁體中文，
教材放在這個資料夾，產出的學習單請用 Word、測驗請用可上線的 GAS Web App。
之後請依這份規則做事。
```

**0-2 OpenCode 接 NVIDIA 免費模型（口頭引導，非貼給 Agent）**
```
到 build.nvidia.com 註冊 → Generate API Key 拿 nvapi-xxx →
OpenCode 執行 /connect 選 NVIDIA 貼上金鑰 → /models 選 DeepSeek V4 Flash
```

---

## 一、兒美：單字表 → 互動遊戲 + 自動收成績

**1-1 生成互動遊戲並上線**
```
這是我這課的單字表（附檔 / 貼在下面）。請幫我做一個「英文單字配對小遊戲」網頁：
左邊英文、右邊中文，學生點擊配對，答對發光、答錯抖動，最後顯示得分。
做成 GAS Web App（純前端即可，不必連試算表），部署後給我短網址 + QR Code。
單字：〈apple, banana, ...〉
```

**1-2 加上自動收成績（用 clasp-gas-guide）**
```
請先 git clone https://github.com/mathruffian-dot/clasp-gas-guide 並照它的指南，
把剛剛的單字遊戲改成「學生輸入座號→玩完把得分寫進我的 Google 試算表」，
部署成 GAS Web App，給我短網址 + QR Code。學生只填座號，不要真名。
```

---

## 二、國中：課文 → 克漏字 / 閱讀測驗 → 上線 → 自動批改

**2-1 課文出題**
```
這是課本第〈5〉課課文（附檔）。請出：
(1) 克漏字 10 題（挖文法與關鍵字）
(2) 閱讀測驗 5 題（單選），每題標注 Bloom 認知層次
另外產出「答案卷 + 詳解」。先給我題目草稿，我確認後再做成網頁。
```

**2-2 部署成可作答的網頁**
```
把剛剛的測驗做成 GAS Web App：學生填座號作答、送出後把作答寫進 Google 試算表。
部署後給我短網址 + QR Code。
```

**2-3 自動批改 + 個別化反饋**
```
讀取那份 Google 試算表的學生作答，幫每位學生自動批改、算分，
並針對「錯最多的文法點」各寫一句個別化建議，整理成一張總表給我。
學生只用座號，不要顯示真名。
```

---

## 三、高中：作文草稿 → 依評分標準即時回饋

**3-1 設定評分標準（rubric）**
```
請把以下作文評分標準記進 AGENTS.md，之後每篇作文都用同一把尺：
內容(5) / 組織(5) / 文法(5) / 用字(5)。
回饋格式：逐項給分 + 各一句具體建議 + 不要直接幫學生重寫整篇。
```

**3-2 批改一篇作文**
```
這是學生的英文作文草稿（貼在下面）。請依 AGENTS.md 的 rubric 逐項評分與回饋，
標出 3 個最該優先改的地方，並示範「怎麼改」其中一句，但不要整篇代寫。
草稿：〈...〉
```

---

## 四、國中會考：考古題的「整理教學」

**4-1 考點診斷**
```
這是近〈5〉年會考英文考古題（附檔 PDF）。請幫我：
(1) 把題目依考點分類（時態、介系詞、閱讀策略…）做成考點地圖
(2) 找出高頻、易錯的題型
(3) 針對每個高頻考點，反推「學生該補的觀念」，不要只是再給一份題目。
```

**4-2 對症變式練習**
```
針對上面整理出的「現在完成式」高頻考點，幫我出 8 題變式練習（由淺到深），
附答案與一句解析，並做成 GAS Web App 收作答。
```

---

## 五、備課加速器：長文章 → 分級教材

**5-1 三難度教材 + 單字表 + 練習**
```
這是一篇英文文章（附檔）。請依 Bloom 三層次產出三種難度的教材：
記憶理解 / 應用 / 分析，各附：重點整理、單字表（單字｜詞性｜KK音標｜例句）、
5 題練習與答案。先給我大綱，我確認後再展開。
```

---

## 六、批改與回饋（減壓）

**6-1 批次初評**
```
這是全班的英文造句作業（附檔 / 試算表）。請幫每位學生先做「初步評語 + 修改建議」，
標出共同錯誤 Top 3 供我課堂講解。最後我會親自確認再發還。
```

---

## 七、行政自動化：學生進度追蹤

**7-1 每週彙整落後名單**
```
以這份 GAS 收進來的 Google 試算表為資料源，幫我每週彙整：
誰沒交、誰分數低於〈60〉，並各自動寫一句鼓勵 / 提醒語，整理成一張表。
（示範就好，先跑這一週。）
```

---

## 八、一句話生簡報 / 影片（三種格式）

**8-1 互動 HTML 簡報（html-slide-builder-lite）**
```
git clone https://github.com/mathruffian-dot/claude-html-slide-builder-lite 後照它的 SKILL，
幫我把「現在完成式」做成 Reveal.js 互動簡報：封面、概念、過去式 vs 完成式對照、
3 題隨堂練習、結語。先列大綱我確認，再產出 index.html。
```

**8-2 SOIL PPTX 簡報（soil-english-deck）**
```
git clone https://github.com/mathruffian-dot/soil-english-deck 後照它的 SKILL，
用 SOIL 六引擎幫我做一份「現在完成式」的 .pptx，對象國中、一節課。
例句用結構卡分色、過去式 vs 完成式用對照表。每顆引擎跑完先讓我確認。
```

**8-3 教學動畫影片 / 科普短片（claude-video-specs-lite）**
```
git clone https://github.com/mathruffian-dot/claude-video-specs-lite 後照它的規範，
幫我做一支 3 分鐘「easy 與 easily 怎麼分」的教學動畫簡報（HTML），
可選配 Edge-TTS 英文旁白。先給我分鏡腳本我確認。
```

---

## 九、聽力 / 字幕（內建技能）

**9-1 課堂錄音轉字幕**
```
把這段英文課堂錄音（附檔）轉成乾淨的 SRT 字幕，時間碼對齊、去掉贅字。
```

---

> 提示詞通用技巧：①先說身分與目標 ②附上真實教材 ③要求「先給大綱 / 草稿再展開」 ④產出格式講清楚（Word / 網頁 / pptx）⑤敏感資料只用座號。
