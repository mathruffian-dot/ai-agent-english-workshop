# 🎓 AI Agent 英文教學實戰工作坊（3 小時）

> **對象**：兒美 / 國中 / 高中英文老師　**時長**：180 分鐘
> **主軸載具**：Anti-Gravity（Google 生態、免費）＋ OpenCode（開源 + NVIDIA 免費雲端 AI）
> **核心精神**：跳過帳號申請的繁瑣，從安裝到「Agent 幫你備課、出題、批改」一條龍
> **設計原則**：前後端 + 資料儲存全用 **純 GAS Web App（Google Apps Script + Google 試算表）**，只要一個 Google 帳號，全程不需 GitHub / Firebase / Netlify（Netlify、Classroom 列為備用資料）

---

## ⏱️ 時間總覽

| 階段 | 內容 | 時間 |
|------|------|------|
| 一 | 觀念翻轉：會聊天的 AI vs 會做事的 Agent | 25 分 |
| 二 | 兩大載具安裝設定：Anti-Gravity ＋ OpenCode | 35 分 |
| ☕ | 中場休息 | 10 分 |
| 三 | 英文教學四大實戰（兒美 / 國中 / 高中 / 會考） | 75 分 |
| 四 | 備課加速器 ＋ 批改回饋 ＋ 行政追蹤 | 25 分 |
| 五 | 收尾：把今天變成你每天的工作流 | 10 分 |

---

## 【第一階段】觀念翻轉：為什麼是「Agent」不是「ChatGPT」(25 分)

* **一句話分水嶺**：生成式 AI 給你**一段話**；AI Agent 幫你產出**一份檔案**。
* **Agent 四大條件**（NN/g 口訣）：目標導向 → 自主行動 → 迭代判斷 → 自我導向。
  * 「它會迭代嗎？它會採取行動嗎？它會自己決定下一步嗎？」
* **對英文老師的意義**：
  * ChatGPT：你要當「複製貼上大師」，把單字表、課文、學生作業手動貼來貼去。
  * Agent：直接讀你電腦裡的課本 PDF、單字表 Word，**自己讀、自己做、自己存檔**。
* **🎬 震撼 Demo**：同一個任務「把這課單字表做成一份聽力填空學習單」，網頁版 ChatGPT vs Anti-Gravity Agent 並排跑，看落差。

---

## 【第二階段】兩大免費載具：Anti-Gravity ＋ OpenCode (35 分)

* **為什麼選這兩款**（先講清楚定位）：

  | | Anti-Gravity | OpenCode |
  |---|---|---|
  | 開發商 | Google | 開源社群（sst） |
  | 費用 | 免費額度（Google 帳號登入）；**有 Pro 帳號可解鎖更多免費額度** | 免費（BYOK，接雲端免費模型即可開跑） |
  | 強項 | Google 生態原生、NotebookLM 整合、內建 Gemini | 不綁品牌、模型可自由切換、雲端方案 CP 值高 |
  | 規則檔 | `AGENTS.md` | `AGENTS.md` |

* **實戰 A：Anti-Gravity 安裝**
  * `antigravity.google.com` → Google 帳號登入 → 下載桌面版
  * **額度說明**：一般 Google 帳號就有免費額度；若已有 **Pro 帳號，會解鎖更多免費額度**，研習中跑 Demo 更不容易卡住。
  * 三區設定速覽：Browser / Apps / Agent 行為
  * 第一個資料夾：把「我的英文教材」資料夾交給它

* **實戰 B：OpenCode 安裝 + 接 NVIDIA 免費雲端 AI**
  * 認明 `sst/opencode` → 下載安裝
  * **馬鞍（Harness）概念**：作業系統固定，模型（CPU）自己換
  * **接模型三步驟（NVIDIA 免費雲端 API）**：
    1. 到 **`build.nvidia.com`** 註冊 **NVIDIA Developer 帳號**
    2. 選一個模型 → 按 **Generate API Key**，拿到 `nvapi-xxx` 格式金鑰
    3. 在 OpenCode 執行 `/connect` → 選 **NVIDIA** → 貼上 `nvapi-xxx` → `/models` 選模型，即可零成本開跑
  * **研習推薦免費模型**（NVIDIA 雲端皆有免費端點）：
    * **DeepSeek V4 Flash** 🥇（284B MoE、1M context，快又適合 agentic 任務）
    * Qwen3-Coder 480B、GLM 5.1、Gemma 4 31B IT 等可備用
  * ❌ 本次**不介紹本地模型**：本地模型對硬體要求太高、架設太麻煩，不適合研習現場快速上手。
  * ℹ️ NVIDIA **開發階段免費**，正式生產才需 NVIDIA AI Enterprise 授權 —— 研習用途完全免費。

* **💰 認識免費的限制 + 未來付費升級選項（NVIDIA 免費 vs OpenCode Go）**：
  > 讓老師明白「免費能用但有邊界」，並提供一個便宜可靠的未來購置選項。

  | 面向 | NVIDIA 免費 API（本次研習用） | OpenCode Go（$10/月，未來選項） |
  |------|------------------------------|-------------------------------|
  | **費用** | 完全免費 | 首月 $5，之後 $10/月 |
  | **模型型態** | 自家 Nemotron + 社群開源模型 | 嚴選經測試的開源程式模型 |
  | **模型精度** | FP16/FP8（接近原版，非激進量化） | 未公開，但經團隊 benchmark 驗證 |
  | **用途限制** | 僅限原型 / 開發測試，不可商用 | 可正式使用、無用途限制 |
  | **資料隱私** | 會被記錄、用於改進產品 | 零留存政策（zero-retention） |
  | **可用性** | 有速率限制，大模型可能排隊 | 穩定可靠，美 / 歐 / 新加坡三地部署 |
  | **模型挑選** | 數量多但未針對 coding 驗證 | 14 個嚴選模型，針對 coding agent 實測 |
  | **速率用量** | 免費配額，可能被限流 | $60/月等值使用量，用完可切 Zen 餘額 |

  * **一句話結論**：研習先用 **NVIDIA 免費**（模型多、正規精度），但記得它**限開發測試用、對話會被記錄**；若日後想穩定、可正式使用又零留存，**OpenCode Go（$10/月，首月 $5）** 是高 CP 值的升級選項。兩者可並存，NVIDIA 端點還能當備援。

* **⚠️ 資訊安全三提醒（務必講清楚）**：
  1. **免費 / 開發者方案的對話資料可能被拿去訓練** —— 別把不能外流的內容丟進去。
  2. **會用到中國大陸開發的模型（如 DeepSeek、Qwen、GLM），但伺服器託管在美國（NVIDIA 雲端）** —— 模型來源與資料落地分屬不同地區，對資安敏感者要先知情。
  3. **學生與個人敏感隱私資料切記不要上傳** —— 真要處理，先做去識別化（只留座號＋班級代號），或改用 Anti-Gravity（Pro）等不拿去訓練的管道。

* **共同設定**：用一份 `AGENTS.md` 寫下「我是英文老師、回應用繁中、教材在哪個資料夾」，讓兩款都能接手。

---

## ☕ 中場休息 (10 分)

---

## 【第三階段】英文教學四大實戰 (75 分)

> 直接對應研習目標的四個族群場景。
> **本階段核心工具**：📊 **純 GAS Web App**（Google Apps Script 同時當前端 + 後端，資料直接寫入 Google 試算表）
> **為什麼用純 GAS**：只要一個 Google 帳號、不必註冊任何新服務、前後端同源沒有 CORS 問題 —— 最符合「跳過帳號申請」的精神。
> （Netlify 部署、Google Classroom 派發列為**備用資料**，本次現場不操作）

### 🛠️ 開場 5 分鐘：純 GAS Web App 怎麼一手包辦
* **前端**：HTML / CSS / JS 寫在 `index.html`，由 GAS `HtmlService` 直接提供，學生用手機就能開。
* **後端**：`Code.gs` 用 `doGet()` / `doPost()` 接請求，直接讀寫 Google 試算表，不必架資料庫。
* **上線**：clasp 部署成網頁應用程式 → Agent 自動縮成短網址 + QR Code 發給學生。
* **🎒 專屬懶人包**：現場直接把 [`clasp-gas-guide`](https://github.com/mathruffian-dot/clasp-gas-guide) 這個 repo `git clone` 下來丟給 Agent，它會照著懶人包**自動完成 clasp 安裝、GAS 部署、縮網址 + QR Code**，老師不用記指令。
* **可選整合**：同一份懶人包也支援讓 GAS 後端接 **Gmail（寄個別化反饋）** 與 **Google 行事曆（自動排考試 / 繳交日）**。

### 🧸 場景一：兒美老師 — 單字表變身互動遊戲並上線 (17 分)
* **輸入**：拖一份單字表（Word / 圖片）給 Agent。
* **請 Agent 做**：
  1. 生成**配對 / 聽力填空互動遊戲**（寫成 GAS Web App 的 HTML 前端）
  2. **部署成 GAS Web App** → Agent 自動縮短網址 + 產生 QR Code，學生手機就能玩
  3. 後端 `.gs` 把學生的遊戲得分**寫進 Google 試算表**，老師即時看到誰玩了、答對幾題
* **產出**：一個短網址 + 自動收集成績的試算表。

### 📖 場景二：國中英文 — 課本段落變測驗並上線 (20 分)
* **輸入**：上傳課本一段課文。
* **請 Agent 做**：
  1. 出**克漏字 + 閱讀測驗**，標注 Bloom 認知層次，自動生成答案卷與詳解
  2. **部署成 GAS Web App**（前端作答頁 + 後端寫入試算表），學生掃 QR Code / 點短網址即可作答
  3. **回收批改**：Agent 讀取 **Google 試算表的作答資料** → 自動批改 → 產出**每位學生的個別化反饋**（哪個文法點要加強）

### ✍️ 場景三：高中英文 — AI 寫作家教 (21 分)
* **流程**：
  1. 老師備好作文題目 + 評分標準（rubric）
  2. 把學生作文草稿（檔案或貼上）交給 Agent
  3. **Agent 依 rubric 逐項回饋**（內容 / 組織 / 文法 / 用字）→ 給**修改建議但不代寫**
  4. 老師最後確認，整理成可發還的評語
* **設計重點**：rubric 寫進 `AGENTS.md` 或一支 Skill，之後每篇作文都套用同一把尺。
* **可選延伸**：用 **純 GAS Web App** 做一個「學生貼草稿 → 即時拿到 AI 建議」的小網頁。

### 🏆 場景四：國中會考考古題的「整理教學」 (12 分)
> 對應研習目標：**讓學生增加實力，而非只有刷題**。
* **輸入**：歷屆會考英文考題 PDF。
* **請 Agent 做**：
  1. 自動**分類考點**（時態 / 介系詞 / 閱讀策略…）建立考點地圖
  2. 找出**高頻易錯題型**，反推「該補的觀念」而非「再刷一遍」
  3. 依考點生成**對症的變式練習** → 可再走場景二的路線做成 GAS Web App 收集作答
* **核心訊息**：Agent 把「刷題」升級成「診斷 → 補強 → 變式」的教學設計。

---

## 【第四階段】備課加速器 ＋ 批改回饋 ＋ 行政追蹤 (25 分)

> 沿用核心工具：📊 純 GAS Web App + Google 試算表

* **備課加速器**：丟一篇長英文文章 → Agent 產出**三種難度（記憶理解 / 應用 / 分析）**教材 + 單字表 + 分級練習題；分級練習題可**做成 GAS Web App**，依程度給不同短網址。
* **一句話生出教學影片 / 動畫簡報**：現場把 [`claude-video-specs-lite`](https://github.com/mathruffian-dot/claude-video-specs-lite) 這個 repo `git clone` 下來丟給 Agent，說「我要做一支〈現在完成式〉的教學動畫簡報」，Agent 會依規範自動產出**瀏覽器可播的 HTML 動畫簡報**（可選配 Edge-TTS 免費旁白、`audio-to-srt` 技能上字幕）。
  * **零付費金鑰**：此研習版已移除需要 API Key 的功能（OpenAI 生圖 / 付費 TTS），圖片用免金鑰 Unsplash、語音用免費 Edge-TTS。
  * **字體自動裝**：Agent 會自動上網安裝源石黑體，老師不必手動處理。
* **一句話生出教學簡報（兩種格式、皆零金鑰）**：
  * **互動 HTML 簡報**：clone [`claude-html-slide-builder-lite`](https://github.com/mathruffian-dot/claude-html-slide-builder-lite) 丟給 Agent → 產出 Reveal.js 互動簡報（CSS 漸層底圖、emoji 圖標、滑桿前後對比）；雙擊瀏覽器即可上課，也可用 GAS Web App 上線（免 Sheets）。
  * **PPTX 簡報（SOIL 教學法）**：clone [`soil-english-deck`](https://github.com/mathruffian-dot/soil-english-deck) 丟給 Agent → 依 SOIL 六引擎產出 `.pptx`，內建時態對照表、例句結構卡、寫作前後對照等**英文科專用版型**（已從數學科版改寫、移除生圖金鑰）。
* **批改與回饋**：把學生作業（檔案或 **GAS 收進來的 Google 試算表**）交給 Agent → 先給**初步評語 + 修改建議** → 老師最終把關，整條批改線減壓。
* **行政自動化（學生進度追蹤）**：以 **GAS 寫入的 Google 試算表**為資料來源，請 Agent **每週彙整誰落後、自動寫一段提醒**（可示範排程化的概念）。
* **隱私底線**（必講）：學生資料**去識別化**（只留座號＋班級代號）；敏感內容不丟免費模型，改走 **Anti-Gravity（Pro）等不拿去訓練的管道**，或留在 **Google 試算表**這類校內既有環境。

---

## 【第五階段】收尾：把今天變成你的日常工作流 (10 分)

* **三句話帶走**：
  1. Agent 不是更會聊天，是**會動手做事**。
  2. 一份 `AGENTS.md` = 你的教學理念，寫一次，兩款工具都聽你的。
  3. 工具會換，但**「駕馭 AI 的能力」是你的**，誰也拿不走。
* **下一步建議**：Anti-Gravity 走 Google 生態日常備課；OpenCode 接 NVIDIA 免費雲端 AI 走零成本路線。
* **Q&A + 留資源清單**。

---

## 🎒 研習前準備清單

* **學員**：
  * 安裝 **Anti-Gravity 桌面版**（Google 帳號登入即可；有 Pro 帳號更佳）
  * 安裝 **OpenCode 桌面版**，並先到 **`build.nvidia.com`** 註冊 **NVIDIA Developer 帳號**取得 `nvapi-xxx` 金鑰
  * 準備一份自己的真實教材（單字表 / 課文 / 學生作文 / 會考考古題擇一）
  * （只要 Google 帳號即可，**不需註冊 Netlify**）
* **講師**：
  * 四大場景的示範教材 + 預期產出範例
  * `AGENTS.md` 英文老師範本、rubric 範本
  * **專屬懶人包 repo**：[`clasp-gas-guide`](https://github.com/mathruffian-dot/clasp-gas-guide) —— 現場 `git clone` 後丟給 Agent，自動完成 clasp 安裝、GAS 部署、縮網址 + QR Code，並可選配 Gmail / 行事曆整合
  * **教學影片 repo**：[`claude-video-specs-lite`](https://github.com/mathruffian-dot/claude-video-specs-lite) —— 研習版（已移除付費 API 功能、字體自動安裝），`git clone` 後丟給 Agent 即可一句話生出教學動畫簡報 / 科普短片
  * **互動簡報 repo**：[`claude-html-slide-builder-lite`](https://github.com/mathruffian-dot/claude-html-slide-builder-lite) —— Reveal.js 互動簡報（零金鑰、可瀏覽器直接開或 GAS 上線）
  * **PPTX 簡報 repo**：[`soil-english-deck`](https://github.com/mathruffian-dot/soil-english-deck) —— SOIL 六引擎英文科簡報技能，產出 `.pptx`（零金鑰）
  * 隱私去識別化假資料組
* **已刻意略過**：**GitHub 與 Firebase** 帳號 —— 前後端與資料儲存全用 **純 GAS Web App + Google 試算表**。
* **備用資料（不現場操作）**：① **Netlify 部署**（想要漂亮網址 / 靜態頁更快時的進階選項）；② **Google Classroom** 的作業派發 / 回收 / 批改流程。皆提供書面說明供有興趣的老師自行延伸。

---

> 本大綱結合 Obsidian 筆記（AntiGravity 基本功、OpenCode 基本功、AI Agent 定義與特徵整理、AI Agents 基本功系列規劃）與專案「研習目標.txt」整理而成。
